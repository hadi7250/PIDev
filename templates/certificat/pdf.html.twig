<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Certificat - {{ certificat.codeUnique }}</title>
  <style>
    @page { margin: 1.2cm; }
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'DejaVu Sans', sans-serif;
      color: #333;
      padding: 30px 40px;
      min-height: 100%;
    }
    .certificate {
      position: relative;
      max-width: 100%;
      min-height: 90%;
    }
    .brand {
      text-align: center;
      font-size: 14px;
      letter-spacing: 0.2em;
      color: #444;
      margin-bottom: 8px;
    }
    .title {
      text-align: center;
      font-size: 32px;
      font-weight: bold;
      letter-spacing: 0.15em;
      color: #2c2c2c;
      margin-bottom: 4px;
    }
    .subtitle {
      text-align: center;
      font-size: 12px;
      letter-spacing: 0.35em;
      color: #555;
      margin-bottom: 28px;
    }
    .presented {
      text-align: center;
      font-size: 14px;
      font-style: italic;
      color: #555;
      margin-bottom: 12px;
    }
    .recipient {
      text-align: center;
      font-size: 22px;
      font-weight: bold;
      color: #1a1a1a;
      margin: 10px 0 8px 0;
      padding: 0 20px;
    }
    .line {
      width: 280px;
      height: 1px;
      background: #888;
      margin: 0 auto 16px auto;
    }
    .achievement {
      text-align: center;
      font-size: 13px;
      font-style: italic;
      color: #555;
      margin-bottom: 50px;
    }
    .signatures {
      position: absolute;
      bottom: 40px;
      left: 0;
      right: 0;
      display: table;
      width: 100%;
    }
    .signature {
      display: table-cell;
      width: 50%;
      text-align: center;
      vertical-align: top;
    }
    .signature-line {
      width: 120px;
      height: 1px;
      background: #666;
      margin: 0 auto 6px auto;
    }
    .signature-name {
      font-size: 10px;
      font-weight: bold;
      letter-spacing: 0.08em;
      color: #333;
    }
    .signature-role {
      font-size: 9px;
      color: #666;
      text-transform: lowercase;
    }
    .deco-tl {
      position: absolute;
      top: 0;
      left: 0;
      width: 60px;
      height: 60px;
      border-left: 2px solid #333;
      border-top: 2px solid #333;
    }
    .deco-tr {
      position: absolute;
      top: 0;
      right: 0;
      width: 60px;
      height: 60px;
      border-right: 2px solid #333;
      border-top: 2px solid #333;
    }
    .deco-bl {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 60px;
      height: 60px;
      border-left: 2px solid #333;
      border-bottom: 2px solid #333;
    }
    .deco-br {
      position: absolute;
      bottom: 0;
      right: 0;
      width: 60px;
      height: 60px;
      border-right: 2px solid #333;
      border-bottom: 2px solid #333;
    }
  </style>
</head>
<body>
  <div class="certificate">
    <div class="deco-tl"></div>
    <div class="deco-tr"></div>
    <div class="deco-bl"></div>
    <div class="deco-br"></div>

    <p class="brand">EDUCONNECT</p>
    <h1 class="title">CERTIFICATE</h1>
    <p class="subtitle">OF COMPLETION</p>
    <p class="presented">proudly presented to</p>

    <p class="recipient">{{ nom }} {{ prenom }}</p>
    <div class="line"></div>
    <p class="achievement">for completing {{ eventTitre }}</p>

    <div class="signatures">
      <div class="signature">
        <div class="signature-line"></div>
        <p class="signature-name">OLIVIA WILSON</p>
        <p class="signature-role">Director</p>
      </div>
      <div class="signature">
        <div class="signature-line"></div>
        <p class="signature-name">ISABEL MERCADO</p>
        <p class="signature-role">Manager</p>
      </div>
    </div>
  </div>
</body>
</html>
